/**
 * Re-export from the calendar module for backward-compatible imports.
 * Prefer `import { TravelCalendar } from "@/components/ui/calendar"` in new code.
 */
export {
  TravelCalendar,
  type TravelCalendarMode,
} from "@/components/ui/calendar";
