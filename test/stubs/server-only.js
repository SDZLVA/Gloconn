/**
 * Test stub for the `server-only` package.
 *
 * Next.js resolves the real package at build time. Under `tsx --test`,
 * Amadeus HTTP modules still `import "server-only"` — this empty module
 * satisfies that import without changing production behavior.
 *
 * Loaded via `test/register-server-only.mjs` (see npm `test` script).
 */

export {};
