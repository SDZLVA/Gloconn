/**
 * Shared TypeScript types for Glooconn.
 * Import from `@/types` — domain-specific types live in separate files.
 */

export type {
  PassengersState,
  SearchData,
  SearchFormErrors,
  SearchFormState,
  TravelersState,
  TravelStyle,
} from "@/types/search";

export {
  INITIAL_PASSENGERS,
  INITIAL_SEARCH_FORM,
  INITIAL_TRAVELERS,
} from "@/types/search";
