/**
 * Budget helpers — currency options, limits, and formatting.
 * Import from `@/lib/budget` in components and hooks.
 */

export {
  BUDGET_LIMITS,
  CURRENCY_OPTIONS,
  formatBudget,
  type CurrencyCode,
} from "@/lib/budget/currencies";
