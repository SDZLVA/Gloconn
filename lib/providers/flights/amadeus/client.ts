/**
 * Amadeus API client — server-only HTTP calls.
 * Implement OAuth2 client-credentials and Flight Offers Search here.
 */

import "server-only";

// export async function searchFlightOffers(
//   request: SearchRequest,
//   credentials: { apiKey: string; apiSecret: string },
// ): Promise<AmadeusFlightOffer[]> { ... }

export {};
